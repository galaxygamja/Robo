# Robo 소프트웨어 구현 계획

## 2026-09-08 조작 이력 저장·재시작 차단 추가

[장부 규격·API·안전 경계](OPERATION_JOURNAL_KO.md). SQLite 기반 명시적 생성/고정 ID 열기,
전송 전 예약·execute-intent 커밋, 미확인 작업의 로봇 단위 차단, 영구 재실행 금지 work_id와
작업자 격리 기록을 추가했다. 두 프로세스의 동시 예약과 프로세스 강제 종료 후 복구를 시험한다.
정지 요청에는 디스크 접근이 없으며, stop ACK로 물리 성공이나 자동 복구를 만들지 않는다.
별도 JournaledManipulatorSender API이며 live runtime은 아직 미연결이다.
팀원과 합의할 CAD/배출 단위/팔 동작은 변경하지 않았다. 구현 종료 후 사용자 요청에 따라 검증 완료 변경을 커밋·공유한다.
검증: 전체 808개 통과, 신규 36개는 영상 의존성 없이도 전부 통과.
배포본 빌드·별도 설치 후 장부 생성/예약/재열기와 정적 검사도 통과했다.

## 2026-09-08 복합 오류 회귀 추가 — 우선순위 2

[세부 실행과 증거 경계](COMPOUND_FAULTS_KO.md). 기존 영상 운영 검증에 `--compound-faults`를
추가했다. 생성 픽셀을 실제 검출·추적·임무·가짜 통신으로 연결하는 8개 시나리오와
검증기의 잘못된 성공 판정 방지 시험을 구현했다. 정상 다중 물체 기준선, 가림,
동일 색 물체 교차, 새 장애물 진입을 이동 요청/ACK 지연 및 stop ACK 유실과 조합한다.
종료 보고서와 종료 후 wire 감시를 분리해 늦은 ACK를 소급하지 않는다.
실제 제어 안전 기준/팀원 코드는 변경하지 않았다. 구현 종료 후 사용자 요청에 따라 검증 완료 변경을 커밋·공유한다.
이전 조작 규격 작업은 `3f43cbd`로 main에 공유했다.
검증: 전체 OpenCV 5 회귀 772개, 신규 전용 20개는 OpenCV 4/5에서 통과.
기존 영상 CLI 3개와 복합 8개의 결합 실행, 소스 배포본에 두 검증 도구 포함도 확인했다.

## 2026-09-08 조작·센서 논리 규격과 가짜 장치 시험 완료

[세부 규격·실행·미구현 경계](MANIPULATOR_PROTOCOL_KO.md)를 따른다. `manipulator_wire`,
`fake_manipulator`, `manipulator_lab`을 추가했다. 보드와 무관한 집기·배출/센서 확인 명령,
중복·지연·타임아웃·취소·정지와 4대 독립 식별을 시험한다. 전용 54개와 전체 752개 회귀 통과.
기존 실제 임무는 합성 센서 근거를 계속 거부한다. 실제 전송/live runtime 조작 연결은 아직 없다.
이후 가림·ID 교차·다중 장애물·통신 오류 복합 통합 회귀를 위 후속 작업으로 추가했다.
센서/기구 안전 상태·재부팅 후 결과 대조와 주행/조작 공통 정지 합의는 별도 남겨둔다.
팀원 작업을 변경하지 않았다. 구현 종료 후 사용자 요청에 따라 검증 완료 변경을 커밋·공유한다.

## 2026-09-07 하드웨어 없는 운영 준비 — 2시간 작업 및 최종 검증 완료

관측 전용 임무 모드, 명시적 물체 선택/초안 도구, 검토된 초기 영역의 원자적 대상 연결과
픽업 전 신원 유실 시 전체 종료를 추가했다. schema 2에서는 명시적 집게 접촉점 오프셋과
새 관측으로 픽업점을 계산하고 변위/재계획 횟수 초과를 차단한다. 기존 검출기/추적기/
WorldState/A*를 재사용한다. 검토한 물체 외곽의 동적 장애물 지도, 제한 재계획,
ACK 대기 중 실제 대기 명령/수신기 모델 목표까지 포함하는 사전 충돌 검사도 추가했다.
최종 검증: 전체 Python 698개를 OpenCV 4/5에서 각각 통과. 영상 통합 시나리오 각각 3개,
OpenCV 4/5의 120초 관측 전용 지속 시험, wheel/sdist 별도 설치·보고서 분석도 통과했다.
[운영 흐름·재현 도구·현재 범위](HARDWARE_FREE_OPERATIONS_KO.md)를 참고한다.
임무 전환 때 이전 픽업점은 현재 사용이 아닌 이력으로 표시하도록 마지막 경계도 검증했다.
작업서까지 갱신했다. 2026-09-08 후속 사용자 요청에 따라 검증 완료 변경을 main에 커밋·공유한다. 팀원 웹 동선/CAD/장치 사양은 수정하지 않았다.

## 2026-09-07 운영·통신 통합 후속

`runtime --mission ... --wire-fake`에서 관측→임무/경로→차동 출력→4대 가짜 수신기를
연결하고, 전체 오류 잠금·정지 요청·명시적 재연결·통신 보고서 분석을 구현했다.
[실행법과 남은 경계](RUNTIME_WIRE_KO.md)를 따른다. 아래 기본 wire 단계의
‘runtime 통신 오류 전파 미구현’은 가짜 전송 모드에 한해 완료로 대체한다.
실제 전송 매체·장치/펌웨어·집게/센서 연결과 현장 검증은 미완료다.

## 2026-09-07 통신 규격·가짜 수신기 후속

`wire_codec`, `wire_sender`, `fake_receiver`, `wire_lab`에 JSON 바이트 규격,
기존 차동 출력 검증, 장치별 ACK/세션/한 번 쓰는 허가/독립 만료 시험을 추가했다.
관측·경로·제어 알고리즘을 중복 구현하지 않는다. [통신 규격·남은 경계](WIRE_PROTOCOL_KO.md)를 따른다.
실제 전송 매체/펌웨어, runtime 전체 통신 장애 처리, 집게·센서 wire 확장은 남아 있다.

## 2026-09-07 임무·경로 실행 후속

`runtime --mission`에 WorldState → 기존 Manipulator → A* 경유점 → 차동 차체 속도
기록을 연결했다. 한 번에 한 로봇의 임무를 실행하고 관측으로 도착을 확인한다.
센서 입력 계약은 제공하지만 실제 센서/모터 전송은 미연결이다. 아래 과거의
‘임무/경로 연결 미구현’은 이 범위에 한해 대체한다.
[실행·검증·남은 범위](MISSION_ROUTE_RUNTIME_KO.md)를 먼저 확인한다.

기준일: 2026-09-04  
대상: 한국 예선 120초 경기  
기본 전략: 고정 상부 카메라(B안) 우선, 드론(A안)은 후속 영상원

## 2026-09-07 최신 작업 판단

장치 합의: **2륜 차동구동/옆 이동 불가 확정**, 엔코더는 미장착 예상(미확정).
[장치·명령 규격](DEVICE_COMMAND_SPEC_KO.md)이 과거 메카넘/엔코더 PID 가정보다 우선한다.
차동 경로 추종·전진/회전 차체 명령은 위 후속 작업의 `runtime --mission`에 구현됐다.
기존 `--goals`/웹 검증의 메카넘 모드와 구분한다. 실제 좌우 모터 출력 변환·보정은 아직 미연결이다.
엔코더가 없다면 카메라 위치 피드백과 실측 모터 출력 보정을 사용하고 바퀴 속도 측정을 가정하지 않는다.

후속 40분 작업으로 원본 체커보드 수집·렌즈 추정·field schema 2/runtime 연결을 추가했다.
[렌즈 실행 안내](LENS_CALIBRATION_KO.md), [작업 기록](LENS_WORK_20260907_KO.md)을 참고한다.

팀원 최신 `1613d57`까지 코드와 Python 272개 기준선 시험을 확인했다.
작업 중 도착한 `7a1c37d`의 의료키트 1·2·1 전담 설정도 반영했다. 카메라 코드와 겹치지 않는다.
아래 9월 5일의 추적/HSV 미구현 표기는 당시 기록이며 현재 작업 배정에 사용하지 않는다.

- **실제 영상용 코드:** 입력·보정·AprilTag·HSV·`PoseTracker`·`ObjectTracker`는 재사용한다.
- **실행 통합:** 새 `robo_control.runtime`이 실제 USB/녹화 프레임을 기존 제어 코어에 연결한다.
  영상 프로세스와 중앙 감시를 분리하며 200ms 관측 만료, 3프레임 재확인, 기록 실패 정지를 적용한다.
- **모의 출력:** 제어 코어는 측정 좌표로 속도를 계산하지만 휠/제동 매개변수는 미검증이다.
  `MockActuatorBank`는 메모리 검증기이며 ESP32 펌웨어나 모터 드라이버가 아니다.
- **시뮬레이션:** 웹 동선/완료 시간, `qualifier.run_mock`, `control_loop --demo` 가상 주행,
  드론 시점 비교는 실물 주행 완료로 계산하지 않는다. 새 runtime에는 가상 위치 적분기가 없다.
- **다음 우선순위:** 현장 카메라/차체/휠 실측과 보드·센서 사양 확정 → 펌웨어 정지/벤치 시험
  → 실제 통신 및 임무/경로 연결. 완료한 추적 기능을 다시 만들지 않는다.

설치/제약은 [카메라 runtime 안내](REAL_CAMERA_RUNTIME_KO.md),
담당 범위/결과는 [이번 작업 기록](REAL_RUNTIME_WORK_KO.md)을 기준으로 한다.
H1 햄스터, H2/B1/B2 비버이며 태그 ID는 유지한다. 실제 단일 runtime은 카메라 한 대를 처리한다.

## 2026-09-05 구현 상태 (과거 기록)

- 동료가 추가한 `robo_control` 패키지와 시뮬레이터를 공통 저장소 기반으로 사용한다.
  아래 초기 설계의 `robo_server` 명칭 대신 현재 패키지명은 `robo_control`이다.
- 실제 영상 처리 첫 구현은 `robo_control.vision`이다. 카메라·영상 파일 입력,
  수동 네 모서리 보정, 픽셀↔mm 변환, 보정 JSON, 독립 기준점 오차 검사,
  프레임 지연·순서·해상도 검사를 구현했다.
- 현재 M2+M3 전체 회귀시험은 vision 의존성 환경에서 Python 172개와 웹 26개를
  통과했다. 영상 파일 디코딩·보정·AprilTag 검출·로그 저장을 포함하며,
  실물 카메라 정확도는 아직 측정하지 않았다.
- M2 전체 통과 조건인 실물 100프레임 보정 확인, 렌즈 파라미터 보정·저장,
  실제 촬영부터 처리까지의 지연 측정은 남아 있다.
- M3 첫 단위인 AprilTag 로봇 ID·위치·방향 검출을 구현했다. 네 로봇의
  구성 가능한 ID, 장착 방향·회전중심 오프셋, 누락·중복·미등록·경기장 밖
  관측 차단, 실제 이미지·영상 CLI와 JSONL 로그를 포함한다.
- 다음 코딩 단위는 HSV 색상 물체 검출과 여러 프레임의 로봇 추적이다. 기존
  엔진에 관측을 연결하는 작업과 ESP32 주행 제어는 아직 구현하지 않았다.
- 설치와 실제 카메라 사용은 [카메라 보정 사용 설명](CAMERA_CALIBRATION_KO.md)을 따른다.

## 팀 작업 중복 방지 인계표

2026-09-07 23:10 KST 기준이며 기존 팀원 작업과 관측·임무·가짜 통신·운영 준비 후속을 포함한다.
코드 완료와 실물 검증 완료를 구분한다. 완료 항목은 새로 만들지 말고 현재 인터페이스를 확장한다.

| 작업 묶음 | 상태 | 팀 작업 기준 |
|---|---|---|
| M2 카메라/영상 입력 | 코드 완료 | `OpenCVCameraSource`, `VideoFileSource`, `CameraFrame` 재사용 |
| M2 네 모서리·mm 보정 | 코드 완료 | `FieldCalibration` 재사용, 실물 정확도 시험만 남음 |
| M2 렌즈 왜곡 보정/원본 사진 수집 | 코드 완료·실물 검증 대기 | `lens`, `lens_capture`, `lens_calibrate`; schema 2 내장 렌즈, 높이 시차/어안 모델은 별도 |
| M2 프레임 안전 검사 | 코드 완료 | `FrameProcessor` 재사용, 검출 후 신선도 재검사 포함 |
| M3 4대 AprilTag 프레임 관측 | 코드 완료 | `AprilTagDetector`와 `config/robot_tags.json` 재사용 |
| M3 인쇄 태그 생성 | 코드 완료 | `tools/generate_robot_tags.py` 사용, 별도 태그 사전 생성 금지 |
| M3 다중 프레임 추적 | 코드 완료 | 기존 `PoseTracker` 재사용; runtime 제어 관측은 200ms로 더 엄격하게 만료 |
| M3 HSV 물체 검출/추적 | 코드 완료 | `ColorDetector`, `ObjectTracker` 재사용; 현장 HSV 검증 필요 |
| 카메라→추적→목표 속도 runtime | 코드 완료·실물 시험 대기 | `runtime`, `runtime_session`, `runtime_io`; 모터 출력 미연결 |
| 비동기 기록/분석/고장 시험 | 코드 완료 | `runtime_report`, `tests/test_runtime*.py`, `tools/verify_camera_runtime.py` |
| 실제 고정 카메라 2대 융합 | 미구현 | 카메라별 보정·동기화·중복 관측 충돌 규칙 필요 |
| WorldState/임무/경로 연결 | 코드 완료·출력 검증 모드 | `world_state`, `mission_runtime`; 단독 이동 점유/차동 경유점 추종, 실물 출력 미연결 |
| 관측 물체→임무 대상 연결 | 코드 완료 | `mission_bindings`, `binding_tools`; 명시 검토/원자적 초기 연결, 신원 유실 시 종료 |
| 관측 기반 픽업 접근점 | 코드 완료 | `observed_pickup`; 검토한 접촉 오프셋/누적 변위/재계획 제한, 센서 성공과 구분 |
| 관측 물체 장애물 지도 | 코드 완료·보수적 제한 | `observed_obstacles`; 외곽 검토 필수, 누락 잠금, 접촉/적재 예외 없음 |
| 차동 명령→4대 통신 오류 전파 | 가짜 통신 모드 완료 | `runtime_wire`, `wire_sender`; 수신기 이전 목표·대기 명령도 전달 전 검사, 실제 통신 미연결 |
| 운영 통합 재현 검증 | 코드 완료 | `tools/verify_mission_operations.py`; 생성 영상 3개 시나리오/선택 관측 지속 시험, 실물 성능 아님 |
| ESP32·실제 모터 폐루프 | 미구현 | 실물 보정과 안전 게이트 통과 전 출력 금지 |

현재 설정은 `hardware_verified=false`, `tag_size_mm=null`이다. 자동시험 통과를
실물 정확도 합격으로 바꾸어 기록하면 안 된다. 좌표계, 태그 방향 또는 JSON
스키마를 변경할 팀원은 기존 시험과 두 사용 설명을 먼저 확인하고 합의한다.

## 1. 결과 목표

최종 소프트웨어는 고정 상부 카메라에서 경기장을 관찰하고, 물체와
로봇을 추적하며, 4대의 지상 로봇(`H1`, `H2`, `B1`, `B2`)이 충돌하지 않도록 임무와 경로를
계산하고, ESP32 로봇에 고수준 명령을 전송해야 한다.

첫 번째 수직 기능 조각(vertical slice)은 다음 한 사이클이다.

1. 시뮬레이터 또는 녹화 프레임을 받는다.
2. 로봇 한 대와 물체 한 개의 상태를 만든다.
3. 물체를 목적지에 배정하고 경로를 계산한다.
4. `NAVIGATE → CAPTURE → TRANSPORT → RELEASE` 상태를 수행한다.
5. 완료 판정과 모든 명령·상태 전환을 로그로 남긴다.

이 사이클이 자동 테스트에서 반복 가능해진 뒤 실제 카메라와 ESP32를
연결한다.

## 2. 범위와 비범위

초기 필수 범위:

- Python 중앙 서버
- 고정 카메라, 영상 파일, 시뮬레이터 입력
- 경기장 원근 보정과 mm 좌표계
- AprilTag 로봇 식별
- HSV 기반 빨강·노랑·초록 검출
- 세계 상태, 임무 상태기계, A* 경로계획
- UDP 기반 서버·로봇 통신
- ESP32 엔코더 PID와 500ms 안전 타임아웃
- 실행 로그와 오프라인 재생
- 1대에서 시작해 2대, 최종 4대로 확장하는 충돌 회피

초기 비범위:

- 생성형 AI 또는 대형 딥러닝 모델
- 클라우드 서버 의존성
- 처음부터 완전 최적 다중 에이전트 경로계획
- 실제 프로토콜을 모르는 상태에서의 드론 제어
- GUI 중심의 운영 프로그램

## 3. 시스템 경계

```text
FrameSource
  ├─ SyntheticSource
  ├─ VideoFileSource
  ├─ FixedCameraSource
  └─ DroneSource (후속)
          │
          ▼
Calibration → Perception → Tracking → WorldState
                                          │
                                          ▼
                                  MissionManager
                                          │
                                          ▼
                              Planner + TrafficManager
                                          │
                                          ▼
RobotGateway ← telemetry ─────────── command
    ├─ SimulatedRobotGateway
    └─ UdpRobotGateway → ESP32 firmware
```

안전 관리자는 영상 입력, 세계 상태, 명령 송신, 텔레메트리를 모두 감시하고
이상이 있으면 새 임무 발급을 중지하고 `SAFE_STOP`을 전송한다.

## 4. 핵심 계약

### 좌표와 시간

- 경기장 원점, X/Y 방향, 회전각 부호를 설정 문서에 고정한다.
- 서버 내부 길이 단위는 mm, 각도 단위는 rad, 시간은 단조 시계 기반 초다.
- 영상 프레임에는 캡처 시각과 처리 시각을 별도로 기록한다.
- 오래된 프레임과 유효시간이 지난 명령은 폐기한다.

### 영상 입력

모든 영상원은 최신 프레임, 캡처 시각, 프레임 번호, 해상도, 연결 상태를
같은 형식으로 제공한다. 인식 모듈은 영상원이 드론인지 웹캠인지 알지 못한다.

### 로봇 명령

초기 전송 형식은 디버깅하기 쉬운 UDP JSON으로 한다. 모든 패킷에
`protocol_version`, `robot_id`, `sequence`, `command`, `ttl_ms`를 포함한다.
목표 이동 명령에는 X/Y/방향과 선속도·회전속도 제한을 포함한다.

ESP32는 패킷 수신 시각을 자체 단조 시계로 기록하고 500ms 동안 유효한
새 명령이 없으면 모터를 정지한다. 연결 복구만으로 이전 동작을 재개하지
않는다.

### 로그와 재생

- 심사용 원본 영상과 분석 오버레이를 분리한다.
- 프레임, 검출, 세계 상태, 임무, 경로, 명령, 응답, 오류를 한 실행 ID와
  같은 시간축으로 연결한다.
- 실패 실행을 실제 로봇 없이 재생할 수 있어야 한다.

## 5. 마일스톤과 통과 조건

### M0 — 기반 구조

산출물:

- Python 패키지, CLI, 설정 예시, 테스트, 정적 검사, CI
- ESP32 PlatformIO 구조
- 핵심 데이터 모델과 어댑터 인터페이스

통과 조건:

- Python 3.11/3.12에서 설치 및 테스트 성공
- `python -m robo_control` 실행 성공
- 하드웨어 값이 설정 파일 밖에 하드코딩되지 않음

### M1 — 결정론적 2D 시뮬레이터

산출물:

- 1,143 × 1,181mm 경기장
- 차동구동 로봇, 물체, 목적지, 충돌 모델
- 고정 seed와 시간 단계 실행
- 헤드리스 테스트와 선택적 OpenCV 표시

통과 조건:

- 같은 입력은 같은 최종 상태와 로그를 생성
- 로봇이 벽과 다른 로봇을 통과하지 않음
- 포획·운반·배출 이벤트를 재현 가능

### M2 — 카메라와 좌표 보정 (코드 부분 완료, 실물 검증 미완료)

산출물:

- 영상 파일·웹캠 입력기 — 구현 완료 (`ef5e020`)
- 네 모서리 원근변환과 픽셀-mm 변환 — 구현 완료 (`ef5e020`)
- 보정 JSON·독립 기준점 오차·프레임 지연 검사 — 구현 완료 (`ef5e020`)
- 렌즈 보정 파라미터와 태그 높이 보정 — 미구현

통과 조건:

- 100프레임 중 99프레임 이상 경기장 변환 성공
- 모서리 순서 반전 없음
- 오래된 프레임을 제어에 사용하지 않음

### M3 — 인식과 추적 (AprilTag 프레임 관측만 코드 완료)

산출물:

- AprilTag ID·중심·방향 검출 — H1/H2/B1/B2 구현 완료 (`ef5e020`)
- 실제 사진·영상 CLI, 인쇄 태그, 합성 투영·PNG·MJPG 회귀시험 — 구현 완료
- HSV 물체 색상·외곽 검출 — 미구현
- 짧은 가림·재획득·500ms 손실 정지를 처리하는 추적기 — 미구현
- 고정 카메라 2대 동기화·중복 관측 융합 — 미구현

통과 조건:

- 로봇 위치 오차 15mm, 방향 오차 5도 이내 목표
- 색별 50회 시험에서 95% 이상 정확도
- 검출되지 않은 대상을 임의 위치에 생성하지 않음

### M4 — 세계 상태와 오프라인 재생

산출물:

- 로봇·물체 수명주기와 신뢰도
- 미처리/예약/포획/운반/완료/분실 상태
- 녹화된 입력으로 전체 상태를 재생하는 도구

통과 조건:

- 같은 녹화와 설정으로 임무 결정이 재현됨
- 가려진 물체가 중복 ID로 즉시 생성되지 않음

### M5 — 단일 로봇 임무·경로·상태기계

산출물:

- 역할 기반 임무 배정
- 장애물 팽창을 적용한 A*
- 웨이포인트 단순화
- 전체 포획 상태기계와 시간 제한
- 105~110초 신규 임무 차단

통과 조건:

- 시뮬레이션 100회에서 교착 없이 종료
- 실패 이유가 `RETRY`, `ABANDON`, `SAFE_STOP` 중 하나로 기록
- 이미 득점한 물체를 장애물·보호 대상으로 취급

### M6 — 통신과 가상 로봇 게이트웨이

산출물:

- 버전이 있는 명령·텔레메트리 스키마
- UDP 게이트웨이와 가상 게이트웨이
- 명령 번호, 중복 제거, TTL, heartbeat
- 지연·손실·재정렬 장애 주입 테스트

통과 조건:

- 중복되거나 오래된 명령을 실행하지 않음
- 통신 손실 후 500ms 이내 정지 상태 확인
- 영상부터 명령 반영까지 200ms 이하 목표를 측정 가능

### M7 — ESP32 펌웨어

산출물:

- 보드·핀 설정 분리
- 엔코더 계측, 바퀴 속도 PID, 서보 게이트
- 포획·게이트·배터리 텔레메트리
- 물리 정지와 통신 watchdog
- 모터 없는 모의 입출력 빌드

통과 조건:

- 바퀴를 든 상태에서 방향·엔코더 부호 검증
- 잘못된 입력과 통신 손실에서 항상 모터 출력 0
- 재연결 시 명시적 새 명령 전까지 정지 유지

### M8 — 실제 로봇 한 대 폐루프

산출물:

- 카메라 위치 보정과 엔코더 제어 결합
- 목표 좌표 이동, 최종 20cm 저속 정렬
- 실제 포획·배출 전체 사이클

통과 조건:

- 1m 이동 오차 30mm 이하
- 포획·배출 10회 중 9회 성공
- 비상정지와 통신 장애 시험 통과

### M9 — 다중 로봇과 제출 안정화

산출물:

- 2대 후 4대 우선순위 예약, 대기점, 좁은 구간 상호배제
- 전체 120초 실행과 촬영 절차
- 필요할 경우 드론 영상 입력기 독립 실험

통과 조건:

- 120초 시험 10회 중 8회 이상 목표 점수
- 기존 득점 물체 훼손 없음
- 한 로봇 장애가 다른 로봇의 안전정지를 방해하지 않음

## 6. 권장 일정과 임계 경로

공식 일정은 반드시 다시 확인한다. 현재 기록의 9월 29일 제출 마감을
기준으로 다음과 같이 시간 제한을 둔다.

| 기간 | 주 작업 | 종료 판단 |
|---|---|---|
| 9/4~9/6 | M0, M1 시작 | 서버 CLI·CI·시뮬레이터 골격 |
| 9/7~9/11 | M1, M2, M3 | 고정 영상에서 좌표·태그·색 검출 |
| 9/10~9/14 | M4, M5 | 시뮬레이션 단일 로봇 전체 사이클 |
| 9/12~9/17 | M6, M7 | 프로토콜·ESP32 안전정지 |
| 9/17~9/21 | M8 | 실제 로봇 한 대 9/10 성공 |
| 9/21~9/24 | M9 | 1대→2대→지상 4대로 단계 확대 |
| 9/24~9/27 | 전체 반복 | 120초 시험 10회와 결함 수정 |
| 9/27~9/28 | 제출 리허설 | 영상·사진·기술 요약 검증 |
| 9/29 | 완충일 | 새 기능 금지, 제출 확인 |

임계 경로는 `고정 카메라 → 단일 로봇 → 포획 성공 → 전체 반복`이다.
드론은 이 경로를 막아서는 안 된다.

## 7. 팀 작업 분할

두 명 이상이면 다음 작업을 병렬로 진행할 수 있다.

- 서버 기반/시뮬레이션: 데이터 모델, 상태기계, 테스트
- 영상: 기존 `robo_control.vision` 위에 HSV, 추적, 2카메라 융합, 실측 데이터 추가
- 펌웨어: 모터·엔코더·서보·watchdog
- 통합/기구: 프로토콜, 측정, 포획 반복시험, 로그 분석

공유 인터페이스 변경은 먼저 작은 설계 PR로 합의한다. 한 PR에서는 가능하면
한 모듈의 계약 또는 한 가지 사용자 동작만 변경한다.

## 8. 첫 GitHub 이슈 묶음

1. `M0: Python 패키지와 CI 구축`
2. `M0: 핵심 좌표·상태 모델 확정`
3. `M1: 결정론적 경기장 시뮬레이터`
4. `[코드 완료 ef5e020 / 실물 검증 미완료] M2: FrameSource와 영상 파일 입력기`
5. `[코드 완료 ef5e020 / 실물 검증 미완료] M2: 경기장 네 모서리 및 원근 보정`
6. `[코드 완료 ef5e020 / 실물 검증 미완료] M3: AprilTag 로봇 검출`
7. `[미구현] M3: 다중 프레임 로봇 추적과 500ms 손실 정지`
8. `[미구현, 병렬 가능] M3: HSV 환자 물체 검출`
9. `[미구현] M3: 고정 카메라 2대 동기화와 관측 융합`
10. `M5: A*와 장애물 팽창`
11. `M5: 단일 로봇 임무 상태기계`
12. `M6: 명령·텔레메트리 프로토콜`
13. `M7: ESP32 watchdog과 안전정지`
14. `M8: 단일 로봇 전체 사이클 시험`

## 9. 완료 정의

기능은 다음을 모두 만족해야 완료로 본다.

- 정상 경로와 실패 경로의 자동 테스트가 있다.
- 설정과 입력 자료로 결과를 재현할 수 있다.
- 하드웨어가 없어도 모의 구현으로 테스트할 수 있다.
- 시간 제한과 안전 동작이 코드에 명시되어 있다.
- 로그만 보고 실패 위치와 이유를 확인할 수 있다.
- README 또는 관련 문서가 함께 갱신된다.
- CI의 테스트와 코드 검사가 통과한다.

## 10. 구현 전 확정해야 할 입력

M0~M1은 아래 정보 없이 진행할 수 있지만, 이후에는 반드시 확정해야 한다.

- Junior/Senior 참가 부문
- 실제 경기장 부속 도면과 목적지 좌표
- 실제 ESP32-S3 보드 모델과 핀 수
- 모터 엔코더 해상도와 감속비
- 바퀴 지름·축간거리 실측값
- 모터 드라이버 종류
- 게이트 서보 동작 범위
- 카메라 모델, 해상도, 설치 높이
- 운영진의 외부 서버·무선통신 답변
